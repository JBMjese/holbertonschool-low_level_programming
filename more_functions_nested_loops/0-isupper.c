#include "main.h"
/**
 * _isdigit - Checks 
 * @c: The number to be checked
 * Return: 1 if the number is a digit, 0 otherwise
 */
int _isupper(int c)

{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
