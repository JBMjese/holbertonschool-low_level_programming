#include <stdio.h>
/**
  * main - Print all the letters except q and e
  * Return: Always 0.
  **/
int main(void)
{
	int numero = 0;

	while (numero <= 9)
	{
		printf("%d ", numero);
		numero++;
	}
	printf("\n");	
	return (0);
}
